# ============================================================================
# Fundr Indexer - Environment Variables Template
# ============================================================================
# Copy this file to:
#   .env.local   - Local development (Anvil)
#   .env.sepolia - Sepolia testnet
#   .env.prod    - Production (mainnet)
#
# NEVER commit real credentials to version control!
# ============================================================================

# Blockchain RPC Endpoints
RPC_HTTP=
RPC_WSS=

# Chain Configuration
CHAIN_ID=
FACTORY=
DEPLOY_BLOCK=

# Database Connection (PostgreSQL)
DATABASE_URL=

# TLS Configuration (set to 0 for self-signed certs in dev)
NODE_TLS_REJECT_UNAUTHORIZED=1

# ============================================================================
# Environment Examples:
#
# LOCAL (anvil, chainId=31337):
#   RPC_HTTP=http://127.0.0.1:8545
#   RPC_WSS=ws://127.0.0.1:8545
#   CHAIN_ID=31337
#   FACTORY=<deployed-factory-address>
#   DEPLOY_BLOCK=0
#   DATABASE_URL=postgresql://localhost:5432/fundr_local
#
# SEPOLIA (chainId=11155111):
#   RPC_HTTP=https://your-sepolia-rpc
#   RPC_WSS=wss://your-sepolia-websocket
#   CHAIN_ID=11155111
#   FACTORY=<sepolia-factory-address>
#   DEPLOY_BLOCK=<deployment-block>
#   DATABASE_URL=postgresql://your-test-db
#
# MAINNET (chainId=1):
#   RPC_HTTP=https://your-mainnet-rpc
#   RPC_WSS=wss://your-mainnet-websocket
#   CHAIN_ID=1
#   FACTORY=<mainnet-factory-address>
#   DEPLOY_BLOCK=<deployment-block>
#   DATABASE_URL=postgresql://your-prod-db
# ============================================================================
